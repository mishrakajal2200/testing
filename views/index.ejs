<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap"
      rel="stylesheet"
    />
    <title>Company Details</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css"
    />
    <script
      src="https://kit.fontawesome.com/a076d05399.js"
      crossorigin="anonymous"
    ></script>
    <style>
      body{
        background-color: lightblue;
      }
      /* Enhanced hover effect on cards */
      .card-hover {
        transition: transform 0.5s ease, box-shadow 0.5s ease,
          transform 0.5s ease, border-radius 0.3s ease;
      }

      /* Stronger hover effect */
      .card-hover:hover {
        transform: translateY(-10px) scale(1.05); /* Increased lift and scaling */
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15); /* Larger shadow for emphasis */
        border-radius: 10px; /* Slightly rounded corners for the hover state */
      }
      .enquiry-card {
            transition: transform 0.3s ease-in-out, box-shadow 0.3s ease-in-out;
        }
        .enquiry-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
        }
         /* Card hover effect */
         .review-card {
            transition: transform 0.3s ease-in-out, box-shadow 0.3s ease-in-out;
        }
        .review-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
        }


    @keyframes blink {
      50% {
        border-color: transparent;
      }
    }
    @keyframes colorChange {
      0% {
        color: red;
      }
      50% {
        color: rgb(0, 255, 255);
      }
      100% {
        color: green;
      }
    }

    .color-change-text {
      animation: colorChange 3s infinite;
    }

    /* Add transition to the form */
.card {
  transition: all 0.6s ease-in-out;
  background-color: 0.5s, transform 0.8s;
}

/* Hover Effect */
.card:hover {
  transform: scale(1.02); /* Slightly enlarge the card */
  box-shadow: 0px 8px 20px rgba(255, 255, 255, 0.2); /* Soft glowing shadow */
}

/* Fade-in effect for list items */
.list-group-item {
  transition: transform 0.3s ease-in-out, background-color 0.3s ease-in-out;
}

/* Hover effect for list items */
.list-group-item:hover {
  transform: translateX(5px); /* Slight right shift */
  background-color: rgba(255, 255, 255, 0.05); /* Subtle background change */
}

/* Smooth fade-in when page loads */
.fade-in {
  opacity: 0;
  transform: translateY(10px);
  animation: fadeInUp 0.8s ease-in-out forwards;
}

/* Keyframes for fade-in effect */
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
    </style>
   
  </head>
  <body>
    <h1 class="text-center mt-4 mb-4">Company Details</h1>

    <div class="container">
      <!-- Basic information -->
        <div class="row justify-content-center mb-5">
          <div class="col-lg-10">
            <div class="card shadow-lg p-4">
              <div class="row justify-content-center align-items-center col-lg-12 d-flex mx-auto">

                <!-- Left Side: Logo, Social Media, Action Buttons -->
                <div class="col-12 col-md-4 d-flex flex-column align-items-center mb-4 mb-md-0 text-center">
                  <!-- Logo Section -->
                  <div class="mb-4">
                    <img src="<%= logoUrl %>" alt="Company Logo" class="img-fluid rounded-circle shadow-lg" style="max-width: 150px" />
                  </div>
      
                  <!-- Social Media Links -->
                  <div class="my-4">
                    <% if (socialMediaLinks) { %>
                      <% if (socialMediaLinks.facebook) { %>
                        <a href="<%= socialMediaLinks.facebook %>" target="_blank" class="btn btn-primary btn-sm mx-1 my-1">
                          <i class="fab fa-facebook-f text-white fs-5"></i>
                        </a>
                      <% } %>
                      <% if (socialMediaLinks.instagram) { %>
                        <a href="<%= socialMediaLinks.instagram %>" target="_blank" class="btn btn-danger btn-sm mx-1">
                          <i class="fab fa-instagram text-white fs-5"></i>
                        </a>
                      <% } %>
                      <% if (socialMediaLinks.linkedin) { %>
                        <a href="<%= socialMediaLinks.linkedin %>" target="_blank" class="btn btn-info btn-sm mx-1">
                          <i class="fab fa-linkedin-in text-white fs-5"></i>
                        </a>
                      <% } %>
                      <% if (socialMediaLinks.twitter) { %>
                        <a href="<%= socialMediaLinks.twitter %>" target="_blank" class="btn btn-dark btn-sm mx-1">
                          <i class="fab fa-twitter text-white fs-5"></i>
                        </a>
                      <% } %>
                      <% if (socialMediaLinks.youtubeChannel) { %>
                        <a href="<%= socialMediaLinks.youtubeChannel %>" target="_blank" class="btn btn-danger btn-sm mx-1">
                          <i class="fab fa-youtube text-white fs-5"></i>
                        </a>
                      <% } %>
                    <% } else { %>
                      <p class="text-muted">No social media links available.</p>
                    <% } %>
                  </div>
      
                  <!-- Action Buttons -->
                  <div class="mt-3">
                    <button class="btn btn-success mx-1 my-1" onclick="saveInformation()">
                      <i class="fas fa-save text-white"></i> Save
                    </button>
                    <button class="btn btn-warning mx-1" onclick="shareViaSMS()">
                      <i class="fas fa-sms text-white"></i> SMS
                    </button>
                    <button class="btn btn-primary mx-1" onclick="shareViaOtherMediums()">
                      <i class="fas fa-share text-white"></i> Share
                    </button>
                  </div>
                </div>
      
                <!-- Right Side: User Details -->
                <div class="col-12 col-md-8 col-lg-6 text-center mx-auto">
                  <h2 class="color-change-text text-center mb-3">Basic Information</h2>
                  <div class="col-lg-7 col-12 mx-auto align-items-center mb-4">
                    <div class="row w-100">
                      <div class="col-12">
                        <p class="d-flex align-items-center text-start text-wrap mb-2">
                          <i class="fas fa-building text-primary fs-5 me-2"></i>
                          <%= companyName %>
                        </p>
                        <p class="d-flex align-items-center text-start text-wrap mb-2">
                          <i class="fas fa-user-tie text-success fs-5 me-2"></i>
                          <%= designation %>
                        </p>
                        <p class="d-flex align-items-center text-start text-wrap mb-2">
                          <i class="fas fa-user text-danger fs-5 me-2"></i> <%= userName %>
                        </p>
                        <p class="d-flex align-items-center text-start text-wrap mb-2">
                          <i class="fas fa-map-marker-alt text-warning fs-5 me-2"></i>
                          <%= location %>
                        </p>
                        <p class="d-flex align-items-center text-start text-wrap mb-2">
                          <i class="fas fa-envelope text-purple fs-5 me-2"></i>
                          <%= email %>
                        </p>
                        <p class="d-flex align-items-center text-start text-wrap mb-2">
                          <i class="fas fa-phone text-info fs-5 me-2"></i>
                          <% if (contacts.length > 0) { %> <%= contacts.join(', ') %> <% } else { %> No contact numbers available. <% } %>
                        </p>
                        <p class="d-flex align-items-center text-start text-nowrap mb-2">
                          <i class="fab fa-whatsapp text-success fs-5 me-2"></i>
                          <% if (Watsapps.length > 0) { %> <%= Watsapps.join(', ') %> <% } else { %> No WhatsApp numbers available. <% } %>
                        </p>
                      </div>
                    </div>
                  </div>
                </div>
              </div> <!-- row -->
            </div> <!-- card -->
          </div> <!-- col-lg-10 -->
        </div> <!-- row -->
    
      

      <!-- Business Details Section -->
      <div class="card bg-dark shadow-lg d-flex justify-content-center mx-auto col-lg-9 mb-5 align-items-center p-4">
        <h2 class="text-warning text-center border-bottom pb-2 mt-4">
          Business Details
        </h2>
      
        <div class="row g-3 p-3">
          <!-- Company Name -->
          <div class="col-12 col-md-6">
            <div class="card shadow-sm hover-shadow-sm p-3">
              <div class="d-flex align-items-center">
                <i class="fas fa-building fs-5 me-3 text-primary"></i>
                <strong class="me-2">Company Name:</strong>
                <span><%= companyName %></span>
              </div>
            </div>
          </div>
      
          <!-- Nature of Business -->
          <div class="col-12 col-md-6">
            <div class="card shadow-sm hover-shadow-sm p-3">
              <div class="d-flex align-items-center">
                <i class="fas fa-briefcase fs-5 me-3 text-success"></i>
                <strong class="me-2">Nature of Business:</strong>
                <span><%= businessNature %></span>
              </div>
            </div>
          </div>
      
          <!-- Establishment Year -->
          <div class="col-12 col-md-6">
            <div class="card shadow-sm hover-shadow-sm p-3">
              <div class="d-flex align-items-center">
                <i class="fas fa-calendar-alt fs-5 me-3 text-warning"></i>
                <strong class="me-2">Establishment Year:</strong>
                <span><%= establishmentYear %></span>
              </div>
            </div>
          </div>
      
          <!-- GST Number -->
          <div class="col-12 col-md-6">
            <div class="card shadow-sm hover-shadow-sm p-3">
              <div class="d-flex align-items-center">
                <i class="fas fa-file-invoice fs-5 me-3 text-danger"></i>
                <strong class="me-2">GST No:</strong>
                <span><%= gstNo %></span>
              </div>
            </div>
          </div>
      
          <!-- Documents -->
          <div class="col-12">
            <div class="card shadow-sm hover-shadow-sm p-3">
              <div class="d-flex align-items-center">
                <i class="fas fa-folder-open fs-5 me-3 text-info"></i>
                <strong class="me-2">Documents:</strong>
                <div>
                  <% if (documents && documents.length > 0) { %>
                    <% documents.forEach(function(documentUrl) { %>
                      <a href="<%= documentUrl %>" target="_blank" class="btn btn-outline-success btn-sm m-1">
                        <i class="fas fa-file-alt"></i> <%= documentUrl.split('/').pop() %>
                      </a>
                    <% }); %>
                  <% } else { %>
                    <span class="text-muted">No documents available.</span>
                  <% } %>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
      
      <!-- Payment and Bank Information -->
      <div class="card mt-5 col-lg-9 shadow-lg d-flex justify-content-center mx-auto mb-5 bg-danger bg-opacity-50 p-3">
        <h2 class="text-center text-info border-bottom pb-3 typing">
          Payment and Bank Information
        </h2>
        <div class="row g-4 p-4">
          <!-- Payment Details -->
          <div class="col-md-6 col-12">
            <div class="card shadow-sm p-3">
              <h4 class="text-center text-secondary border-bottom pb-2">
                Payment Details
              </h4>
              <ul class="list-group list-group-flush">
                <li class="list-group-item d-flex flex-row align-items-center fade-in">
                  <i class="fab fa-paypal fs-5 me-2 text-info"></i>
                  <strong class="me-1">Paytm No:</strong>
                  <span class="text-break"><%= paytmNo %></span>
                </li>
                <li class="list-group-item d-flex flex-row align-items-center text-start fade-in">
                  <i class="fas fa-mobile-alt fs-5 me-2 text-success"></i>
                  <strong class="me-1">PhonePe No:</strong>
                  <span class="text-break"><%= phonePayNo %></span>
                </li>
                <li class="list-group-item d-flex flex-row align-items-center text-start fade-in">
                  <i class="fab fa-google fs-5 me-2 text-danger"></i>
                  <strong class="me-1">GPay No:</strong>
                  <span class="text-break"><%= gpayNo %></span>
                </li>
                <li class="list-group-item d-flex flex-row align-items-center text-start fade-in">
                  <i class="fas fa-qrcode fs-5 me-2 text-warning"></i>
                  <strong class="me-1">UPI ID:</strong>
                  <span class="text-break"><%= upiId %></span>
                </li>
              </ul>
            </div>
          </div>
      
          <!-- Bank Details -->
          <div class="col-md-6 col-12">
            <div class="card shadow-sm p-md-3 p-2">
              <h4 class="text-center text-secondary border-bottom pb-2">
                Bank Details
              </h4>
              <ul class="list-group list-group-flush">
                <li class="list-group-item d-flex flex-md-row flex-row align-items-start fade-in">
                  <div class="d-flex align-items-center">
                    <i class="fas fa-university fs-5 me-2 text-primary"></i>
                    <strong class="me-2">Bank Name:</strong>
                  </div>
                  <span class="text-wrap ps-md-2"><%= bankName %></span>
                </li>
                <li class="list-group-item d-flex flex-md-row flex-row align-items-start fade-in">
                  <div class="d-flex align-items-center">
                    <i class="fas fa-wallet fs-5 me-2 text-secondary"></i>
                    <strong class="me-2">Account Type:</strong>
                  </div>
                  <span class="text-wrap ps-md-2"><%= accountType %></span>
                </li>
                <li class="list-group-item d-flex flex-md-row flex-row align-items-start fade-in">
                  <div class="d-flex align-items-center">
                    <i class="fas fa-map-marker-alt fs-5 me-2 text-info"></i>
                    <strong class="me-2">Branch Name:</strong>
                  </div>
                  <span class="text-wrap ps-md-2"><%= branchName %></span>
                </li>
                <li class="list-group-item d-flex flex-md-row flex-row align-items-start fade-in">
                  <div class="d-flex align-items-center">
                    <i class="fas fa-id-card fs-5 me-2 text-success"></i>
                    <strong class="me-2 ">Account No:</strong>
                  </div>
                  <span class="text-wrap  text-truncate"><%= accountNo %></span>
                </li>
                <li class="list-group-item d-flex flex-md-row flex-row align-items-start fade-in">
                  <div class="d-flex align-items-center">
                    <i class="fas fa-user fs-5 me-2 text-danger"></i>
                    <strong class="me-2">Account Holder Name:</strong>
                  </div>
                  <span class="text-wrap ps-md-2"><%= accountHolderName %></span>
                </li>
                <li class="list-group-item d-flex flex-md-row flex-row align-items-start fade-in">
                  <div class="d-flex align-items-center">
                    <i class="fas fa-code fs-5 me-2 text-warning"></i>
                    <strong class="me-2">IFSC Code:</strong>
                  </div>
                  <span class="text-wrap ps-md-2"><%= ifscCode %></span>
                </li>
              </ul>
            </div>
          </div>
          

        </div>
      </div>
      
      
      <!-- images gallary-->
      <div class="card mt-5 col-lg-9 shadow-lg d-flex bg-light justify-content-center mb-5 mx-auto p-4">
        <h2 class="text-center text-warning border-bottom pb-2">Images</h2>
      
        <div class="row g-3 justify-content-center">
          <% if (galleryImages && galleryImages.length > 0) { %>
            <% galleryImages.forEach(function(imageUrl, index) { %>
              <div class="col-lg-3 col-md-4 col-sm-6">
                <div class="card shadow-sm border-0 rounded overflow-hidden">
                  <img
                    src="<%= imageUrl %>"
                    class="card-img-top img-fluid rounded"
                    alt="Image <%= index + 1 %>"
                  />
                  <div class="card-body text-center">
                    <p class="card-text fw-bold text-primary mb-0">
                      Image <%= index + 1 %>
                    </p>
                  </div>
                </div>
              </div>
            <% }); %>
          <% } else { %>
            <p class="text-center text-danger">No images available.</p>
          <% } %>
        </div>
      </div>
      
      <!-- all video -->
      <div class="card mt-5 col-lg-9 shadow-lg d-flex bg-light justify-content-center mb-5 mx-auto p-4">
        <h2 class="text-center text-warning border-bottom pb-2">Videos</h2>
        <div class="row g-4">
          <% if (videoUrl && videoUrl.length > 0) { %>
            <% videoUrl.forEach(function(videoUrl) { 
                // Convert YouTube URL to embed format
                let embedUrl = videoUrl.replace("watch?v=", "embed/").replace("youtu.be/", "www.youtube.com/embed/");
            %>
            <div class="col-lg-4 col-md-6 col-sm-12">
              <div class="card shadow-sm border-0 bg-white p-2">
                <div class="ratio ratio-16x9">
                  <iframe src="<%= embedUrl %>" allowfullscreen frameborder="0"></iframe>
                </div>
              </div>
            </div>
            <% }); %>
          <% } else { %>
            <p class="text-center text-danger">No videos available.</p>
          <% } %>
        </div>
      </div>
      
      <!-- all products -->
      <div class="card mt-5 col-lg-9 shadow-lg  justify-content-center mb-5 mx-auto p-4">
        <h2 class="text-center text-warning border-bottom pb-2 mb-4">
          Products
        </h2>
      
        <div class="row g-4">
          <% products.forEach(function(product) { %>
          <div class="col-lg-4 col-md-6 col-sm-12">
            <!-- Product Card with hover effect -->
            <div class="card shadow-lg border-0 bg-info bg-opacity-10 p-3 h-100 card-hover">
              <div class="card-body d-flex flex-column">
                <!-- Product Name -->
                <h5 class="card-title mb-1 font-weight-bold fs-6">
                  <%= product.productName %>
                </h5>
      
                <!-- Price -->
                <p class="card-text text-success font-weight-bold fs-6 mb-1">
                  <strong>Price:</strong> $<%= product.productPrice %>
                </p>
      
                <!-- Type -->
                <p class="card-text text-muted fs-6 mb-0">
                  <strong>Type:</strong>
                  <span class="text-primary"><%= product.productType %></span>
                </p>
      
                <!-- Product Images -->
                <% if (product.productImages && product.productImages.length > 0) { %>
                <div class="product-images my-3">
                  <% product.productImages.forEach(function(imageUrl, index) { %>
                  <div class="mb-3">
                    <img
                      src="<%= imageUrl %>"
                      alt="<%= product.productName %> Image <%= index + 1 %>"
                      class="img-fluid rounded"
                    />
                  </div>
                  <% }); %>
                </div>
                <% } else { %>
                <p>No images available.</p>
                <% } %>
              </div>
            </div>
          </div>
          <% }) %>
        </div>
      </div>
      
      <!-- enquiry and review forms -->
      <div class="g row d-flex flex-md-row col-lg-10 justify-content-center mx-auto align-items-center g-2">
    
        <!-- Inquiry Form -->
        <div class="col-12 col-md-6 d-flex justify-content-center">
          <div class="card p-4 shadow-lg enquiry-card w-100" style="max-width: 500px;">
            <h2 class="text-center text-primary mb-4">Enquiry Form</h2>
            <form>
              <!-- Name Field -->
              <div class="mb-3">
                <div class="input-group">
                  <span class="input-group-text bg-primary text-white border-0">
                    <i class="fa fa-user"></i>
                  </span>
                  <input type="text" id="name" name="name" class="form-control border-0 shadow-sm" placeholder="Enter your name" required>
                </div>
              </div>
        
              <!-- Email Field -->
              <div class="mb-3">
                <div class="input-group">
                  <span class="input-group-text bg-danger text-white border-0">
                    <i class="fa fa-envelope"></i>
                  </span>
                  <input type="email" id="email" name="email" class="form-control border-0 shadow-sm" placeholder="Enter your email" required>
                </div>
              </div>
        
              <!-- Phone Number Field -->
              <div class="mb-3">
                <div class="input-group">
                  <span class="input-group-text bg-success text-white border-0">
                    <i class="fa fa-phone"></i>
                  </span>
                  <input type="tel" id="phone" name="phone" class="form-control border-0 shadow-sm" placeholder="Enter your phone number" required>
                </div>
              </div>
        
              <!-- Message Field -->
              <div class="mb-3">
                <div class="input-group">
                  <span class="input-group-text bg-warning text-white border-0">
                    <i class="fa fa-comment"></i>
                  </span>
                  <textarea id="message" name="message" class="form-control border-0 shadow-sm" placeholder="Enter your message" required></textarea>
                </div>
              </div>
        
              <!-- Submit Button -->
              <button type="submit" class="btn btn-primary w-100 shadow-sm">
                <i class="fa fa-paper-plane"></i> Submit Enquiry
              </button>
            </form>
          </div>
        </div>
        
        <!-- Review Form -->
        <div class="col-12 col-md-6 d-flex justify-content-center">
          <div class="card p-4 shadow-lg border-0 w-100" style="max-width: 500px;">
            <h2 class="text-center text-primary mb-4">Review Form</h2>
            <form>
              <div class="mb-3">
                <div class="input-group">
                  <span class="input-group-text bg-primary text-white border-0"><i class="fa fa-user"></i></span>
                  <input type="text" name="name" class="form-control border-0 shadow-sm" placeholder="Your Name" required>
                </div>
              </div>
              <div class="mb-3">
                <div class="input-group">
                  <span class="input-group-text bg-danger text-white border-0"><i class="fa fa-envelope"></i></span>
                  <input type="email" class="form-control border-0 shadow-sm" placeholder="Your Email" required>
                </div>
              </div>
              <div class="mb-3">
                <div class="input-group">
                  <span class="input-group-text bg-black text-white border-0"><i class="fa fa-heading"></i></span>
                  <input type="text" class="form-control border-0 shadow-sm" placeholder="Review Title" required>
                </div>
              </div>
              <div class="mb-3">
                <div class="input-group">
                  <span class="input-group-text bg-warning text-white border-0"><i class="fa fa-star"></i></span>
                  <select class="form-select form-control border-0 shadow-sm" required>
                    <option value="" disabled selected>Select Rating</option>
                    <option value="5">5 - Excellent</option>
                    <option value="4">4 - Very Good</option>
                    <option value="3">3 - Good</option>
                    <option value="2">2 - Fair</option>
                    <option value="1">1 - Poor</option>
                  </select>
                </div>
              </div>
              <div class="mb-3">
                <div class="input-group">
                  <span class="input-group-text bg-success text-white border-0"><i class="fa fa-comment"></i></span>
                  <textarea class="form-control border-0 shadow-sm" placeholder="Write your review here" required></textarea>
                </div>
              </div>
              <button type="submit" class="btn btn-primary w-100 shadow-sm">
                <i class="fa fa-paper-plane"></i> Submit Review
              </button>
            </form>
          </div>
        </div>
      </div>
    
      <!-- google map -->
      <div class="card mt-5 col-lg-9 shadow-lg  justify-content-center mb-5 mx-auto p-4">
        <h2 class="text-center text-primary mb-4">Location Map</h2>
      
        <div class="map-container mb-4">
          <!-- Google Maps Embed -->
          <iframe
            src="<%= googleMap %>"
            class="w-100"
            style="height: 400px; border: 0;"
            allowfullscreen=""
            loading="lazy"
          ></iframe>
        </div>
      
        <div class="text-center">
          <a
            href="<%= googleBusiness %>"
            target="_blank"
            class="btn btn-outline-primary me-2 my-2"
          >
            Leave a Review
          </a>
          <a
            href="https://your-profile-share-link.com"
            target="_blank"
            class="btn btn-outline-secondary"
          >
            Share Profile
          </a>
        </div>
      </div>
      
    </div>

   

    

  

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
